---
import { getCollection } from "astro:content";
import meetingHands from "../assets/external/meeting-hands.jpg";
import CtaSection from "../components/CtaSection.astro";
import FeatureSection from "../components/FeatureSection.astro";
import SectionHeading from "../components/SectionHeading.astro";
import TestimonialCarousel from "../components/TestimonialCarousel.astro";
import { ROUTES, TESTIMONIALS_CONTENT, WE_CAN_HELP } from "../consts";
import MainLayout from "../layouts/MainLayout.astro";

const testimonialsEntries = await getCollection("testimonials");

const testimonialData = testimonialsEntries.map((entry) => ({
  author: entry.data.author,
  role: entry.data.role,
  body: entry.body?.trim().replace(/^"|"$/g, "") || "",
}));
---

<MainLayout title={TESTIMONIALS_CONTENT.title}>
  <FeatureSection
    title={TESTIMONIALS_CONTENT.title}
    paragraphs={[TESTIMONIALS_CONTENT.description]}
    imageSrc={meetingHands}
    imageAlt="Collaborative team meeting"
    level={1}
    reverse={true}
  />

  <div class="section-padding bg-[var(--bg-muted)]">
    <div class="container-site">
      <TestimonialCarousel testimonials={testimonialData} />
    </div>
  </div>

  <CtaSection
    title={WE_CAN_HELP.title}
    description={WE_CAN_HELP.description}
    primaryLabel={WE_CAN_HELP.primaryLabel}
    primaryHref={ROUTES.CONTACT}
  />
</MainLayout>
